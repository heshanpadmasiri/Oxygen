syntax = "proto3";
package oxygen;

service Oxygen {
    rpc Hello(Req) returns (Res);
}

message Req {
    string msg = 1;
    uint32 id = 2;
}

message Res {
    string msg = 1;
    bool successful = 2;
}
